<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/background_bottomsheet_rounded">

    <!-- Thanh tiêu đề -->
    <TextView
        android:id="@+id/text_title_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Thêm món mới"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="#000"
        android:layout_marginBottom="8dp"/>

    <!-- Thẻ sản phẩm (giống item_cart) -->
    <include
        layout="@layout/cart_item_edit_food" />

    <!-- Ví dụ: Size chọn -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Size (chọn 1)"
        android:textStyle="bold"
        android:textSize="14sp"
        android:textColor="#666"
        android:layout_marginTop="12dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_checkboxes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never" />

    <!-- Nút thêm vào giỏ -->
    <Button
        android:id="@+id/button_add_to_cart"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Add to Cart"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:backgroundTint="#A71317"
        android:layout_marginTop="16dp"/>

</LinearLayout>
